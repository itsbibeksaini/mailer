import { SidebarOptionSelectorDirective } from './sidebar-option-selector.directive';

describe('SidebarOptionSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarOptionSelectorDirective();
    expect(directive).toBeTruthy();
  });
});

import { ReadMailDirective } from './read-mail.directive';

describe('ReadMailDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadMailDirective();
    expect(directive).toBeTruthy();
  });
});

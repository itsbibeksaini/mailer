import { MailSelectorDirective } from './mail-selector.directive';

describe('MailSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new MailSelectorDirective();
    expect(directive).toBeTruthy();
  });
});

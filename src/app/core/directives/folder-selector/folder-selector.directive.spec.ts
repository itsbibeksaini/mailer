import { FolderSelectorDirective } from './folder-selector.directive';

describe('FolderSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new FolderSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
